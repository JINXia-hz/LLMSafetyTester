# 存储深度整合重构调研（第二轮）——现行存储架构的权威记录

> **调研日期**：2026-08-17（数据库重构两阶段落地当日，commit `09af450`）
> **执行记录（同日）**：P1-P7 全部落地——P1 速赢+死代码（`c4b80c6`）、P2 elo 表化+锁家族退役
> （`b61c758`，净删 259 行）、P3 json 链清除（`405838f`，fork=库级 clone/快照=db 副本）、
> P4 真相转正（`b5d236d`，meta.json/trials.jsonl 退役）、P5 control 全层库化（`f45f53e`，
> AtomicIndexStore/fsig/thresholds CLI 清零）、P6 守卫单源化（`7959191`，白名单 23→0）、
> P7 统一库（`81a3023`，R 并入 catalog.db，"唯一真相"叙事退役）。
> **执行记录补遗（2026-08-17/18）**：P8 存储治理收口（`05a3f9a`：task_logs 并入
> gc-tasks、model_state 类别、safe_twins 轮转、predictor_cache 真 LRU）；
> 全库审计修复（`0a72c6b`：Elo/R 观测正确性、prune 接线真 LRU、rstore legacy
> json 整链删除）；**P9 所有权翻转**（`4febec6`：写入口全生命周期 register→
> finalize→remove、查询纯读、reconcile 降级为显式恢复工具、trials/仪表盘任务/
> runits/probes 四处双真相合拢、ctl_queue 重启恢复、写而不读输出物退役）；
> 测试封闭性（`7c7f8d2`：output 全路径隔离）与 CI dispose 竞态修复（`6bf84da`）。
> 累计净删约 5500 行；估算外的额外收益：封驳令重启不丢（安全修复）、
> list_plans 重启可见、MCP 失实门修复、排队 Plan 重启恢复。
> **动机**：第一轮重构是"替代它现在怎么操作数据"——DB 侧迁就旧代码（双格式、双写、兼容分支），旧代码侧留下垃圾。本轮按新原则重设计：**从代码流程真正需要什么入手，数据库与代码深度结合，坚决不留冗余和死代码。**
> **调研方法**：两轮并行只读扫描（control 层 / llmsec 侧）+ 重构过程中的第一手认知；所有结论带 file:line。
> §1/§3 为当时的诊断与计划（历史记录，不删改）；**§2 已更新为当前达成的终态**——与代码不符时以代码为准。

---

## 0. 判断框架：数据到底需要什么

把全部持久化状态按**本质需求**分五类，每类的正确形态不同：

| 类别 | 本质需求 | 正确形态 | 现状形态 |
|---|---|---|---|
| 可变权威状态 | 事务、并发安全、崩溃恢复 | SQLite 表（单事务写） | R✅已库化 / tasks❌ / plans❌ / tickets❌ |
| append-only 事件流 | 只增、时序、按 key 聚合查询 | SQLite 表 + 索引 | gazette❌ / trials❌（jsonl+索引双写） |
| 派生缓存 | 可重建 + 失效键 | 表或文件皆可，但生命周期要统一 | elo_cache❌（json+手写锁）/ predictors✅（文件合理） |
| 大块产物（blob） | 人读、流式 tail、体积大 | **文件永远是真相** | 报告/attack_results/progress✅ 保持文件 |
| 进程内并发原语 | 锁、条件变量、闭包回调 | **永远内存，不是存储问题** | bus 订阅 / confirm 闭包✅ 保持内存 |

第一轮的问题：把第 1、2 类留在了文件形态（用锁模拟事务），又为第 3、4 类在 DB 侧造了兼容层。

---

## 1. 病根清单

### A. 双写 / 双格式冗余（迁移保守的代价）

| # | 问题 | 位置 | 删除前提 |
|---|---|---|---|
| A1 | **tasks 三层结构**：meta.json（真相）+ tasks 表（镜像）+ reconcile 对账 | `task_manager.py:247-279` 双写、`catalog.py:415-466` 对账 | db 转正为真相：PID 已在行内，崩溃终态用"running 行 + PID 探活"推断（`task_store.py:207-209` 已有此逻辑）；.log/.progress.jsonl 保留（流式产物） |
| A2 | **trials 双写**：trials.jsonl（真相，裸 append）+ trials 表（索引，**无对账**——崩溃后行永停 running） | `study.py:202-204` / `executor.py:66-104` | db 转正：register/update 从事务写入转唯一真相，resume 改读表；需补崩溃终态推断 |
| A3 | **ResultsMatrix 双格式 save/load**：.db 走 SQLite、.json/.bak 走遗留 JSON；`_migrate_legacy` 自导入 + `_as_db_path` 映射 | `results.py:368-397`、`rstore.py:90-181` | fork 改 db 级 clone（见 A4）+ 旧 workspace 一次性迁移后，分支整删 |
| A4 | **fork 链六步绕圈**：db→导 json→子进程→复制→子进程→json→db 自动导入 | `workspace.py:32-95`、`snapshot.py:88-89` | `rstore.clone()`（ATTACH+INSERT..SELECT）经 contract 导出；snapshot 的 tar.gz/elo_cache 附带对 control 全冗余 |
| A5 | **elo_cache.json + 手写锁 RMW**：`_file_lock`+`LockTimeout`+`_REENTRY`（95 行）仅剩 elo_cache 两个用户 | `results.py:82-176`、`elo_access.py:96,226` | elo_cache 变 results.db 派生表（事务化）→ 锁家族整删，fingerprint 保留为命中键 |
| A6 | register_run + reconcile 双机制 | `catalog.py:153-244` | runner 登记转正为真相（产物清单入行），reconcile 降级为 reindex 修复工具 |
| A7 | dir_size 双实现 | `common.py:30` + `storage/models.py` | 合一 |
| A8 | task_logs（caches）与 gc-tasks 双清理路径，且前者不看状态**可能清掉 running 任务日志** | `caches.py:83-87` vs `management/storage.py:117-195` | 并入 gc-tasks |
| A9 | **AtomicIndexStore ×3**：workspace/gazette/env_snapshot 各一份 `_index.json` + filelock 跨进程 RMW + corrupt 自愈 | `store.py`（127 行）、`locks.py` | 三张表化（经 contract 扩展）；locks.py 只剩 .env 一个用户 |

### B. 死代码 / 死重（直接删）

1. `core/__init__.py` 12 个路径常量 re-export——全仓无人从包层导入（仅 harmbench 用 4 个不重绑符号）。
2. `RUN_NAME_RE/RUN_ARTIFACTS` 两跳 re-export：data_query 从 management.runs 导（唯一理由是历史），权威已在 storage.catalog。
3. `task_store._read_meta` + `_scan_detached` 裸文件兜底（迁移完成后无 meta.json 的旧任务不复存在）。
4. `extract_elo_fields` 别名（control/compare.py:98）——`extract_report_metrics` 超集；`approve_block`（block.py:73-75）纯别名。
5. gazette `__pending__` 过滤已死（dialogue 注释自认不再用，磁盘残件一并清）。
6. `io.write_csv` 单调用方；`iter_jsonl`/`load_done_ids` 各 2 个外部用户（濒死，可收编）。
7. `data_query` 的 `_run_summary/_run_meta/_RUN_META_CACHE/_file_sig`（~90 行）——与库行 `Run.metrics` 功能重复，趋势/富化应直接吃库行；`_discover_runs_cached` 已是空壳。
8. `ELOTracker.save(filepath=None)` 静默跳过分支无人用。
9. snapshot 的 tar.gz 分支、`.snapshot_staging`（control 从不用）。
10. 白名单 B 类 14 条目：只冻结"永不重绑"常量（PROJECT_ROOT/OUTPUT_DIR 等）——守卫把"隔离敏感"与"静态路径"混为一谈。

### C. 失实 / 真 bug（立即修）

1. **MCP 失实门（真 bug）**：`mcp/tools/query.py:145,218` 用 `RESULTS_FILE.exists()` 判"有无评估数据"——阶段 2 后新部署只产 results.db，**有数据也报"尚无评估数据"**。
2. `list_plans` 只扫内存 `_PLANS` 不扫磁盘——重启后 MCP list_plans 返回空；plan.py docstring 与现实矛盾。
3. capabilities.py:266 / snapshot.py:210 文案停留在 results.json 时代。
4. `.gitignore` 的 `experiments/`（无前导斜杠）误伤 `llmsec/experiments/` 新文件——应为 `/experiments/`。
5. `_TICKETS` 内存态：重启即静默放行全部封驳（executor 的 ticket=None→重试语义）——落库是**安全修复**。

### D. 内存态判定（audit 长期档"13 处"逐个裁决）

| 状态 | 裁决 | 依据 |
|---|---|---|
| `_TICKETS`（封驳令） | **落库**（安全修复） | 8 字段纯数据；TTL 即过期条件 |
| `_QUEUE`（plan 队列） | 内容落库，worker 线程协议留内存 | 重启恢复排队 plan |
| `_SESSIONS` | 可库化但收益低 | `_trim` 的 OpenAI 配对规则与 `_SESSION_LOCKS` 是业务/并发逻辑，不随存储走 |
| `_PENDING`（confirm） | **不可库化** | `execute_fn` 零参闭包不可序列化——除非重造两步确认执行模型 |
| bus `_BUS`/`_recent` | 订阅/同步派发**不可库化**；`_recent` 落库可选 | 回调闭包 + 请求-应答同步 RPC 是进程内语义 |

### E. 非原生实现（从流程需要重设计，不是 1:1 替代）

1. **run 发现靠文件嗅探**（RUN_ARTIFACTS 九种文件 exists）——runner 明知自己在创建什么，却让读方猜。理想：runner 事务化写 runs 行（含产物清单/状态），文件嗅探只留 reindex 修复工具。
2. **task 终态靠 meta.json mtime 对账**——task_manager 直接持有状态，应直接写行。
3. **gazette 搜索靠 500 文件全文读 + SigCache + 文本缓存**——SQL 一句话（66 行事件，LIKE 足够）；`_load_gazette_texts`、planner 的 50-plan 重放循环全删。
4. **thresholds 经 subprocess CLI + `_FALLBACK_THRESHOLDS` 复制体 + TTLCache**（review.py:36-54）——control 实际已直接 import llmsec 四处，纯常量 import 没有隔离收益。
5. **invoker 启发式解析 CLI stdout JSON**（`res.json`）——fork 走 contract 后消失。
6. **isolation monkey-patch 16 常量 + AST 白名单 23 条**——理想：storage 持路径上下文对象，isolation 缩成 swap 上下文 + 日志 handler 切换（唯一不可替点）；守卫只拦"重绑集"。代价：~15 处测试 monkeypatch 改造。
7. `list_workspaces` 每次全目录 du——runner 产物体积无法从 db 得知，保留但可增量维护。

---

## 2. 终态架构（P9 后已达成，现行权威描述）

**所有权原则：db 独占状态，文件只做制品/blob/进程通道。**
写入口全生命周期维护库行（register 创建 → finalize 收尾富化 → remove 删行）；
查询是纯 SELECT（不扫目录、零变更不开写锁）；`reconcile_*` 仅为显式恢复工具
（`storage reindex` / `verify` / `migrate-layouts` / TUI legacy 视图 / workspace
历史采用），服务"手动拷贝/删除目录"这类旁路操作的事后矫正。

```
output/state/
  catalog.db      统一库（唯一数据库文件；P7 起 R 并入，P9 补 probes）
                  · observations / runits / rmodels —— R 观测与评级单位目录
                    （db 真相；runner publish 时 set_units 落库）
                  · elo_cache —— Elo 派生缓存表（fingerprint 命中键，事务失效）
                  · runs / trials / tasks —— 登记三表（db 真相）
                  · probes —— 模型防御指纹（原 state/probes.json 表化，P9）
                  · predictor_cache —— 预测器真 LRU 登记（blob 仍在
                    predictors/*.pkl，文件名即指纹）
                  · ctl_events / ctl_plans / ctl_plan_meta / ctl_tickets /
                    ctl_queue / ctl_workspaces / ctl_env_snapshots —— control 层
                    （封驳令重启不丢；排队 Plan 重启回填，done 行滚动清理）
workspaces/<n>/catalog.db —— 卫星库（fork / work-dir 隔离；sqlite backup API clone）

文件（永真，不入库）：报告/树/md、attack_results.jsonl、progress.jsonl、.log、
  predictors pkl、cluster pkl、prescreen joblib、.env、safe_twins.jsonl（append 流）
```

**P8/P9 退役清单**（代码已不存在，文档不得再引用）：
meta.json（任务）、trials.jsonl（追加写入）、probes.json、alerts.jsonl 文件通道
（告警走 logger + webhook）、sampler_log.jsonl、workspace gc_log 哨兵行、
rstore legacy json 通道（matrix_from_legacy_json / export_legacy_json）、
contract 别名（search_gazettes / backup_results / close_results_db）、
task_logs 缓存类别（并入 `storage gc-tasks`）、predictors_legacy 类别。

边界原则不变：control 仍不 import llmsec 内部——`storage.contract` 是唯一公开面
（零别名），control 经 `control.core.storage` 薄转发消费；AST 守卫继续强制
（sqlite3/sqlalchemy/sqlmodel 仅限 storage 包内）。

---

## 3. 分批实施计划（激进但每批独立可回退）

| 批次 | 内容 | 删除估算 | 风险 |
|---|---|---|---|
| P1 速赢 | C 类全部（MCP 门/list_plans/文案/.gitignore）+ B 类 1-9 死代码 | ~200 行 | 低 |
| P2 elo 表化 | A5：elo_cache → results.db 表；_file_lock/LockTimeout/_REENTRY 退役；caches.elo_cache 类别删 | ~180 行 | 低（派生可重建） |
| P3 json 链清除 | A3/A4：snapshot=backup 导 db、fork=rstore.clone、merge 双读删、migrate-r 用完即删、json 分支删 | ~250 行 | 中（control 同步改） |
| P4 真相转正 | A1/A2：tasks db 真相（meta.json 退役+探活推断）、trials db 真相（jsonl 退役+resume 改读表）；A6/A7/A8 | ~200 行 | 中（崩溃恢复语义） |
| P5 control 库化 | A9+D：contract 扩展建表能力；gazette/plans/tickets/queue/三索引入库；search_history 走 SQL；thresholds 直读 | ~500 行 | 中高（表多、control 全层） |
| P6 路径上下文 | E6：storage 持路径上下文；isolation 缩成 swap；守卫收窄到重绑集（白名单 23→~5） | ~150 行 | 中高（15 处测试改造） |

> **补遗**：后续追加批次均已落地——P7 统一库（`81a3023`：R 并入 catalog.db，
> "唯一真相"叙事退役）、P8 存储治理收口（`05a3f9a`）、P9 所有权翻转
> （`4febec6`，见 §2）。§1 病根清单中 A1-A9/B/C/D/E 各项已全部闭环或按
> 裁决保留（confirm/bus 内存态维持"D 不可库化"裁决）。

合计估算：**删 ~1400 行冗余/死代码，新增 ~500 行**；每批结束全量测试 + verify。

---

## 4. 决策点（需要拍板）

1. **P4 语义变化**：meta.json/trials.jsonl 退役后，"持有进程崩溃"的终态推断从文件 mtime 对账改为 PID 探活——接受吗？（旧 meta.json 文件保留只读，不再写。）
2. **P5 范围**：control 全层库化（gazette/plans/tickets/queue/三索引一次做完）还是先做 gazette+plans（收益最大两块）？
3. **P6 时机**：路径上下文重造动 15 处测试，放在最后独立一批还是并入 P5？
4. **confirm/bus 的内存态**：按调研属"不可库化的真实约束"，本轮不动——认可这个裁决吗？

---

## 5. 附：第一手踩坑记录（本轮实施时必须遵守）

- Windows 上引擎持句柄时删/改名库文件会 PermissionError——回滚路径必须先 `storage_db.close()`。
- 并发首开 WAL 库有 `-shm` 竞态——creator 全局锁 + 重试（db.py 已内建）。
- 测试污染真实库/真实产物的窗口：任何新写入路径必须确认 conftest 隔离覆盖——
  现已隔离 CATALOG_DB、TASK_LOG_DIR 与 output 全部派生路径（cluster 六件、
  孪生、OUTPUT_DIR 本身，`7c7f8d2`）。曾实际发生：runner 全局模式测试经
  `_should_refresh_features` 覆写真实聚类产物、safe_twin 测试覆写真实
  allergy__<模型>.json 报告。
- 后台线程（Plan 队列 worker）活过测试 teardown 时，与引擎 dispose 存在
  连接池竞态（Set changed size during iteration）——teardown 先等队列收敛，
  `db.close()` dispose 带重试（`6bf84da`）。
- merge 目标引导序：upsert 前必须先 `_load_R(target)` 触发遗留导入（P3 清除遗留链后此序一并消失）。
