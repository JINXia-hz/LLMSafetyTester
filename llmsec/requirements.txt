# LLM 红队攻击测试工具依赖（Python 3.11）
# 按 llmsec/ 包实际 import 生成

# 核心：目标/裁判/生成模型 API 与配置
openai>=2,<3
python-dotenv>=1,<2
requests>=2,<3
urllib3>=2,<3

# 本地模拟模型服务器 + Web 面板 (local_model_server.py / dashboard_api.py)
fastapi>=0.100,<1
uvicorn>=0.20,<1
pydantic>=2,<3
jinja2>=3,<4               # FastAPI 模板渲染
pandas>=2,<4               # Web 面板数据聚合

# 聚类分析 (cluster.py)
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
hdbscan>=0.8,<0.9          # 可选：--method hdbscan（默认聚类算法）
sentence-transformers>=2   # 可选：语义嵌入特征
tiktoken>=0.5              # 可选：token 数估算，缺失时退化为近似值
