# pip-audit 漏洞白名单（被 audit.yml 每周阻断车道消费）
#
# 格式：每行一个漏洞 ID（GHSA-xxxx / PYSEC-xxxx），行内 # 后写豁免原因与复审截止。
# 纪律：每条豁免必须有明确原因（不影响当前版本 / 等待上游修复 / 无利用路径），
# 复审截止过期后应移除本条或升级依赖，不允许无限期滞留。
#
# 示例：
# GHSA-xxxx-yyyy-zzzz  # 仅影响 <2.0 分支，当前锁定 2.3 不受影响；复审 2026-12-31
#
#（当前为空——出现需豁免的漏洞时再添加）
