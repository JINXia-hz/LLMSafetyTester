# LLM 红队攻击测试工具——核心依赖（不含聚类可选依赖）
# 聚类可选依赖见 requirements-cluster.in（hdbscan/sentence-transformers/tiktoken）
# 安装方式：
#   pip install -r llmsec/requirements.txt            # 核心（不含 torch）
#   pip install -r llmsec/requirements-cluster.txt    # 完整（含聚类 + torch）
#   pip install -e .                                   # 同核心（pyproject.toml）
#   pip install -e ".[cluster]"                        # 同完整

# 核心：目标/裁判/生成模型 API 与配置
openai>=2,<3
python-dotenv>=1,<2
requests>=2,<3
urllib3>=2,<3

# Web 面板 + 本地模拟模型服务器
fastapi>=0.100,<1
uvicorn>=0.20,<1
pydantic>=2,<3
jinja2>=3,<4
pyyaml>=6,<7
python-multipart>=0.0.6  # 文件上传（攻击集拖拽导入）

# ELO/评估数学（核心管线必需——SVD-Ridge 预测、收敛判据）
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10

# 实验框架
optuna>=3,<4

# 控制层（control/）跨进程文件锁——workspace/env_snapshot/gazette 的索引 RMW
filelock>=3,<4
