# 完整依赖（核心 + 聚类可选依赖）
# 安装：pip install -r llmsec/requirements-cluster.txt
# 或：pip install -e ".[cluster]"
#
# sentence-transformers 会拉入 torch（~2GB），仅聚类特征提取需要。
# 只做攻击评估（不跑聚类分析）的用户用 requirements.txt 即可。

-r requirements.in

# 聚类可选依赖
hdbscan>=0.8,<0.9
sentence-transformers>=2
tiktoken>=0.5
